---
import { format } from "date-fns";

const { date } = Astro.props;

interface Props {
	date: Date;
}
---

<time datetime={date.toISOString()}>
	{format(date, "PP")}
</time>

<style>
	time {
		font-style: italic;
	}
</style>
